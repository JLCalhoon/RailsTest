
<%= form_with(model: victory) do |form| %>
  <% if victory.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(victory.errors.count, "error") %> prohibited this victory from being saved:</h2>

      <ul>
        <% victory.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :title, class: "form-label" %>
    <%= form.text_field :title, class: "form-control"  %>
  </div>

  <div class='mb-3'>
    <%= form.label :content, class: "form-label" %>
    <%= form.text_area :content, class: "form-control" %>
  </div>

  <div>
    <%= form.number_field :userid, id: :victory_user_id, value:current_user.id, type: :hidden%>
  </div>

  <div>
    <%= form.submit class: "btn btn-primary" %>
  </div>
<% end %>

